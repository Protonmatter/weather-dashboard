# Delivery pipeline — RFC 0001 §4.
#
# Each job answers one question. A red build should tell you *what kind* of thing broke
# before you open the log: static, unit, regression, contract, functional, smoke,
# dependency, or budget.

name: CI

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Nightly. Contract and dependency jobs exist to catch drift that happens on someone
    # else's schedule: provider schemas and CVE disclosures.
    - cron: "17 6 * * *"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"
  CLOUDFLARE_PAGES_PROJECT: "protonmatter-weather-dashboard"

jobs:
  static:
    name: Static analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck

  unit:
    name: Unit + regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - name: Unit, validation and regression suites
        run: npm test -- --reporter=verbose
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-results
          path: test-results/
          if-no-files-found: ignore

  dependencies:
    name: Dependency + licence
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - name: Audit and licence allow-list
        run: npm run deps
      - name: Fail on lockfile drift
        run: git diff --exit-code package-lock.json

  build:
    name: Build + budget + smoke
    runs-on: ubuntu-latest
    needs: [static, unit]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - run: npm run build
      - name: Bundle size budget
        run: npm run size
      - name: Smoke test the built artefact
        run: npm run smoke
      - uses: actions/upload-artifact@v4
        with: { name: dist, path: dist, retention-days: 7 }

  e2e:
    name: Functional (E2E)
    runs-on: ubuntu-latest
    needs: [build]
    strategy:
      fail-fast: false
      matrix:
        project: [chromium, webkit, iphone, android]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - name: Install browser
        run: npx playwright install --with-deps
      - run: npm run build
      - name: Run ${{ matrix.project }} journeys
        run: npx playwright test --project=${{ matrix.project }}
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-${{ matrix.project }}
          path: playwright-report/
          retention-days: 7

  contract:
    name: Provider contracts
    # Network-dependent by design. Runs on main and nightly, not on every PR, so an
    # upstream hiccup cannot block an unrelated contributor's pull request.
    if: github.event_name == 'schedule' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: npm }
      - run: npm ci
      - name: Validate live provider schemas
        run: npm run contract
        env:
          RUN_CONTRACT_TESTS: "1"

  deploy:
    name: Deploy to Pages
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: [build, e2e, dependencies]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # Deploy the exact artefact already built, budgeted, smoke-tested, and exercised by
      # E2E. Rebuilding here would make the release subtly different from the evidence.
      - uses: actions/download-artifact@v4
        with: { name: dist, path: dist }
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with: { path: dist }
      - id: deployment
        uses: actions/deploy-pages@v4

  deploy-cloudflare:
    name: Deploy to Cloudflare Pages
    # Dual-host alongside GitHub Pages. Skips itself cleanly until the two repository
    # secrets exist (CLOUDFLARE_API_TOKEN with Pages:Edit, CLOUDFLARE_ACCOUNT_ID) so a
    # fork or a fresh clone is not red for lack of someone else's credentials.
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: [build, e2e, dependencies]
    runs-on: ubuntu-latest
    outputs:
      deployed: ${{ steps.creds.outputs.present }}
      page_url: ${{ steps.cloudflare.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        # Wrangler 4.120.0 requires Node 22; keep this deploy-only runtime separate
        # from the application's Node 20 build and validation baseline.
        with: { node-version: "22", cache: npm }
      - name: Check for Cloudflare credentials
        id: creds
        env:
          TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          ACCOUNT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -n "$TOKEN" ] && [ -n "$ACCOUNT" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "Cloudflare secrets absent - skipping. See README 'Deploying'."
          fi
      - if: steps.creds.outputs.present == 'true'
        run: npm ci
      - if: steps.creds.outputs.present == 'true'
        uses: actions/download-artifact@v4
        with: { name: dist, path: dist }
      - if: steps.creds.outputs.present == 'true'
        name: Create project if absent, then deploy
        id: cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          STATUS=$(curl -sS -o /dev/null -w '%{http_code}' \
            -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/pages/projects/$CLOUDFLARE_PAGES_PROJECT")
          case "$STATUS" in
            200) ;;
            404) npx --yes wrangler@4.120.0 pages project create "$CLOUDFLARE_PAGES_PROJECT" --production-branch=main ;;
            *) echo "Cloudflare project lookup failed with HTTP $STATUS"; exit 1 ;;
          esac
          npx --yes wrangler@4.120.0 pages deploy dist \
            --project-name="$CLOUDFLARE_PAGES_PROJECT" \
            --branch=main \
            --commit-hash="$GITHUB_SHA" \
            --commit-message="${GITHUB_SHA:0:12} verified CI artefact"
          echo "page_url=https://${CLOUDFLARE_PAGES_PROJECT}.pages.dev/" >> "$GITHUB_OUTPUT"

  post-deploy-smoke:
    name: Post-deploy smoke
    needs: [deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Verify the deployed site responds and mounts
        # Fetching index.html alone passes while the site renders blank — the first real
        # deploy served a shell whose entry chunk 404'd under the Pages subpath. The probe
        # must fetch the entry script the shell actually references.
        run: |
          URL="${{ needs.deploy.outputs.page_url || 'https://protonmatter.github.io/weather-dashboard/' }}"
          echo "probing $URL"
          for i in 1 2 3 4 5; do
            HTML=$(curl -fsS --max-time 20 "$URL" || true)
            if echo "$HTML" | grep -q 'id="root"'; then
              SRC=$(echo "$HTML" | grep -o 'src="[^"]*\.js"' | head -1 | sed 's/^src="//;s/"$//')
              case "$SRC" in
                http*) ASSET="$SRC" ;;
                /*)    ASSET="$(echo "$URL" | sed -E 's#^(https?://[^/]+).*#\1#')$SRC" ;;
                *)     ASSET="${URL%/}/${SRC#./}" ;;
              esac
              echo "entry chunk: $ASSET"
              if curl -fsS --max-time 20 -o /dev/null "$ASSET"; then
                echo "post-deploy smoke: PASS"
                exit 0
              fi
              echo "shell served but entry chunk failed"
            fi
            echo "attempt $i failed; retrying"
            sleep 10
          done
          echo "post-deploy smoke: FAIL"
          exit 1

  post-deploy-cloudflare-smoke:
    name: Post-deploy Cloudflare smoke
    if: needs.deploy-cloudflare.outputs.deployed == 'true'
    needs: [deploy-cloudflare]
    runs-on: ubuntu-latest
    steps:
      - name: Verify the Cloudflare site responds and mounts
        env:
          URL: ${{ needs.deploy-cloudflare.outputs.page_url }}
        run: |
          set -euo pipefail
          echo "probing $URL"
          for i in 1 2 3 4 5; do
            HTML=$(curl -fsS --max-time 20 "$URL" || true)
            if echo "$HTML" | grep -q 'id="root"'; then
              SRC=$(echo "$HTML" | grep -o 'src="[^"]*\.js"' | head -1 | sed 's/^src="//;s/"$//')
              case "$SRC" in
                http*) ASSET="$SRC" ;;
                /*)    ASSET="$(echo "$URL" | sed -E 's#^(https?://[^/]+).*#\1#')$SRC" ;;
                *)     ASSET="${URL%/}/${SRC#./}" ;;
              esac
              echo "entry chunk: $ASSET"
              if curl -fsS --max-time 20 -o /dev/null "$ASSET"; then
                echo "Cloudflare post-deploy smoke: PASS"
                exit 0
              fi
            fi
            echo "attempt $i failed; retrying"
            sleep 10
          done
          echo "Cloudflare post-deploy smoke: FAIL"
          exit 1
